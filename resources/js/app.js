import './bootstrap'

import '../css/app.scss'

import * as bootstrap from 'bootstrap'

// Ejemplo de uso de un componente de Bootstrap (por ejemplo, un modal)
// porque no funciona la libreria sin usar un componente XD
const myModal = new bootstrap.Modal(document.getElementById('myModal'));

console.log('JS LOADED')
